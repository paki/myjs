describe('Factory: Socket', () => {

    // load the controller's module
    beforeEach(angular.mock.module('app'));

    var socket;

    // beforeEach(inject((Socket) => {
    //     socket = Socket;
    // }));
    //
    // it('初期状態', (done) => {
    //     expect(socket).toBeDefined();
    //     socket.conn.disconnect();
    //     done()
    // });

    // afterEach(() => socket.conn.disconnect());
});
