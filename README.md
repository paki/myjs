# myjs

```bash
$ git clone git@github.com:sakamossan/myjs.git
$ cd myjs/ && npm install
$ (cd ui/ && npm install && gulp)
$ (cd ui/ && npm install && gulp)  # ? Go well when you retry ?
$ ./dev.sh
```

